# Machine-specific settings
config/settings.local.json

# Logs
logs/
*.log

# Session data (don't sync)
projects/
history.jsonl
debug/
file-history/
session-env/
shell-snapshots/
paste-cache/
plans/
tasks/
todos/
statsig/
ide/
stats-cache.json

# Built-in plugins (install via npm)
# Note: We now sync plugin manifests only
# Exclude marketplace repos (can be re-registered) and cache (can be re-downloaded)
plugins/marketplaces/
plugins/cache/

# Slack webhook config (contains sensitive URL)
.slack-config

# OS files
.DS_Store
Thumbs.db

# Config examples (use actual files in config/)
config/*.json.example
config/*.example
